#include<iostream>
#include<vector>
using namespace std;

int main(){
    // create vector
    vector<int> arr;
    // int ans = (sizeof(arr)/sizeof(int));
    // cout<<ans;
    arr.push_back(5);
    arr.push_back(6);
    arr.push_back(7);

    // for(int i=0; i<arr.size(); i++){
    //     cout<<arr[i]<<endl;
    // }

    // // cout<<sizeof(arr);

    // arr.pop_back();

    // for(int i=0; i<arr.size(); i++){
    //     cout<<arr[i]<<endl;
    // }

    // int n;
    // cout<<"Enter value of n : ";
    // cin>>n;
    // vector<int>array(n,10);
    // cout<<"size of array vector is : "<<array.size()<<endl;
    // cout<<"capacity of array vector is : "<<array.capacity()<<endl;

    // for(int i=0; i<array.size(); i++){
    //     cout<<array[i]<<endl;
    // }

    // cout<<"size of array vector is : "<<array.size()<<endl;
    // cout<<"capacity of array vector is : "<<array.capacity()<<endl;


    // vector<int>crr{10,20,30,40,50};
    // for(int i=0;i<crr.size();i++){
    //     cout<<crr[i]<<" ";
    // }

    // cout<<"Vector crr is empty or not : "<<crr.empty();


    vector<int>drr;
    cout<<"Vector crr is empty or not : "<<drr.empty();

    

    return 0;
}